{
  "name": "copilot-gmail-connector",
  "displayName": "Copilot Gmail Connector",
  "description": "Gmail integration for GitHub Copilot Chat — browse, read, search, compose and manage emails from VS Code",
  "version": "0.1.0",
  "publisher": "copilot-minimax",
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Chat"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onView:gmail-inbox",
    "onChatParticipant:copilot-gmail-connector.gmail"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.93.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.4"
  },
  "contributes": {
    "configuration": {
      "title": "Gmail Connector",
      "properties": {
        "gmailConnector.clientId": {
          "type": "string",
          "default": "",
          "description": "Google OAuth2 Client ID (from Google Cloud Console)"
        },
        "gmailConnector.clientSecret": {
          "type": "string",
          "default": "",
          "description": "Google OAuth2 Client Secret"
        },
        "awsConnector.accessKeyId": {
          "type": "string",
          "default": "",
          "description": "AWS Access Key ID"
        },
        "awsConnector.secretAccessKey": {
          "type": "string",
          "default": "",
          "description": "AWS Secret Access Key"
        },
        "awsConnector.region": {
          "type": "string",
          "default": "us-east-1",
          "description": "AWS Region (e.g. us-east-1, eu-west-1)"
        },
        "vercelConnector.token": {
          "type": "string",
          "default": "",
          "description": "Vercel API Token (from vercel.com/account/tokens)"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gmail-connector",
          "title": "Gmail",
          "icon": "media/gmail-icon.svg"
        }
      ]
    },
    "views": {
      "gmail-connector": [
        {
          "id": "gmail-inbox",
          "name": "Inbox"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "gmail-inbox",
        "contents": "Connect your Gmail account to browse, search and manage emails right from VS Code.\n[Connect Gmail](command:gmail-connector.connect)\n\nYou need a Google Cloud OAuth Client ID.\nSee [Setup Guide](command:gmail-connector.openSetup) for instructions."
      }
    ],
    "commands": [
      {
        "command": "gmail-connector.connect",
        "title": "Connect Gmail Account",
        "category": "Gmail",
        "icon": "$(mail)"
      },
      {
        "command": "gmail-connector.disconnect",
        "title": "Disconnect Gmail Account",
        "category": "Gmail"
      },
      {
        "command": "gmail-connector.refresh",
        "title": "Refresh",
        "category": "Gmail",
        "icon": "$(refresh)"
      },
      {
        "command": "gmail-connector.compose",
        "title": "Compose Email",
        "category": "Gmail",
        "icon": "$(edit)"
      },
      {
        "command": "gmail-connector.readEmail",
        "title": "Read Email",
        "category": "Gmail"
      },
      {
        "command": "gmail-connector.openSetup",
        "title": "Open Setup Guide",
        "category": "Gmail"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "gmail-connector.refresh",
          "when": "view == gmail-inbox",
          "group": "navigation"
        },
        {
          "command": "gmail-connector.compose",
          "when": "view == gmail-inbox",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "gmail-connector.readEmail",
          "when": "view == gmail-inbox && viewItem == email",
          "group": "inline"
        }
      ]
    },
    "chatParticipants": [
      {
        "id": "copilot-gmail-connector.gmail",
        "name": "gmail",
        "fullName": "Gmail Assistant",
        "description": "Manage your Gmail — read, search, compose, reply, and organize emails",
        "isSticky": false,
        "commands": [
          {
            "name": "inbox",
            "description": "Show recent inbox emails"
          },
          {
            "name": "search",
            "description": "Search emails by query"
          },
          {
            "name": "read",
            "description": "Read a specific email by ID or subject"
          },
          {
            "name": "compose",
            "description": "Compose a new email"
          },
          {
            "name": "reply",
            "description": "Reply to an email"
          },
          {
            "name": "labels",
            "description": "List your Gmail labels"
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "gmail_add_account",
        "toolReferenceName": "gmail-add",
        "displayName": "Add Gmail Account",
        "icon": "$(add)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Add a new Gmail account",
        "modelDescription": "Add a new Gmail account with a label (e.g. 'personal', 'work', 'client'). Opens browser for Google OAuth. After adding, this becomes the active account. Accounts persist GLOBALLY on disk — they work in all VS Code windows. Always ask the user for a label. Before adding, call gmail_list_accounts to check if the account already exists from another window.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string",
              "description": "Label for this account (e.g. 'personal', 'work', 'client', 'company-name')"
            }
          },
          "required": [
            "label"
          ]
        }
      },
      {
        "name": "gmail_list_accounts",
        "toolReferenceName": "gmail-accounts",
        "displayName": "List Gmail Accounts",
        "icon": "$(accounts-view-bar-icon)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List all connected Gmail accounts",
        "modelDescription": "Lists all connected Gmail accounts with labels, emails, and which is active. Accounts persist GLOBALLY across all VS Code windows (stored on disk). IMPORTANT: Always call this tool BEFORE telling a user an account isn't connected — accounts may exist from other windows or sessions.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "gmail_switch_account",
        "toolReferenceName": "gmail-switch",
        "displayName": "Switch Gmail Account",
        "icon": "$(arrow-swap)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Switch active Gmail account",
        "modelDescription": "Switch the active Gmail account by label. All subsequent Gmail operations will use this account. Use when user says 'check my work email', 'switch to client account', etc. Call gmail_list_accounts first if you don't know available labels.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string",
              "description": "Account label to switch to (e.g. 'personal', 'work')"
            }
          },
          "required": [
            "label"
          ]
        }
      },
      {
        "name": "gmail_remove_account",
        "toolReferenceName": "gmail-remove",
        "displayName": "Remove Gmail Account",
        "icon": "$(trash)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Remove a connected Gmail account",
        "modelDescription": "Removes a connected Gmail account by label and deletes its stored token. Confirm with user before removing. If this was the active account, switches to another available account.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "label": {
              "type": "string",
              "description": "Account label to remove"
            }
          },
          "required": [
            "label"
          ]
        }
      },
      {
        "name": "gmail_connection_status",
        "toolReferenceName": "gmail-status",
        "displayName": "Gmail Status",
        "icon": "$(info)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Check Gmail connection status",
        "modelDescription": "Shows all connected Gmail accounts and which is active. Accounts persist GLOBALLY on disk across all VS Code windows. Call this before claiming any account isn't connected.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "gmail_check_inbox",
        "toolReferenceName": "gmail-inbox",
        "displayName": "Check Gmail Inbox",
        "icon": "$(mail)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Check your Gmail inbox for recent emails",
        "modelDescription": "Fetches recent Gmail inbox emails. Returns subject, sender, date, snippet, message ID, and a summary of all connected accounts. Uses active account unless 'account' parameter specifies a label. If user says 'check my work email', set account to 'work'. Accounts persist globally across VS Code windows.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "maxResults": {
              "type": "number",
              "description": "Maximum emails to return (default: 15)"
            },
            "query": {
              "type": "string",
              "description": "Optional Gmail search query (e.g. 'is:unread', 'from:john')"
            },
            "account": {
              "type": "string",
              "description": "Account label to use (e.g. 'personal', 'work'). Omit to use active account."
            }
          }
        }
      },
      {
        "name": "gmail_search_emails",
        "toolReferenceName": "gmail-search",
        "displayName": "Search Gmail",
        "icon": "$(search)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Search your Gmail for specific emails",
        "modelDescription": "Searches Gmail using Gmail search syntax. Supports 'from:', 'subject:', 'is:unread', 'has:attachment', 'after:2025/01/01'. Uses active account unless 'account' param specifies a label.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Gmail search query"
            },
            "maxResults": {
              "type": "number",
              "description": "Max results (default: 10)"
            },
            "account": {
              "type": "string",
              "description": "Account label to use. Omit for active account."
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "gmail_read_email",
        "toolReferenceName": "gmail-read",
        "displayName": "Read Gmail Message",
        "icon": "$(file-text)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Read the full content of a Gmail message",
        "modelDescription": "Reads full email content by message ID. Returns from, to, date, subject, body, labels. Uses active account unless specified.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "messageId": {
              "type": "string",
              "description": "Gmail message ID"
            },
            "account": {
              "type": "string",
              "description": "Account label. Omit for active account."
            }
          },
          "required": [
            "messageId"
          ]
        }
      },
      {
        "name": "gmail_send_email",
        "toolReferenceName": "gmail-send",
        "displayName": "Send Gmail",
        "icon": "$(send)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Send an email from your Gmail",
        "modelDescription": "Sends a new email. Requires to, subject, body. Always confirm with user before sending. Uses active account unless specified.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "to": {
              "type": "string",
              "description": "Recipient email address"
            },
            "subject": {
              "type": "string",
              "description": "Email subject"
            },
            "body": {
              "type": "string",
              "description": "Email body text"
            },
            "account": {
              "type": "string",
              "description": "Account label to send from. Omit for active account."
            }
          },
          "required": [
            "to",
            "subject",
            "body"
          ]
        }
      },
      {
        "name": "gmail_reply_to_email",
        "toolReferenceName": "gmail-reply",
        "displayName": "Reply to Gmail",
        "icon": "$(reply)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Reply to a Gmail message",
        "modelDescription": "Replies to an existing Gmail message. Requires messageId and body. Confirm with user before sending.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "messageId": {
              "type": "string",
              "description": "Message ID to reply to"
            },
            "body": {
              "type": "string",
              "description": "Reply body text"
            },
            "account": {
              "type": "string",
              "description": "Account label. Omit for active account."
            }
          },
          "required": [
            "messageId",
            "body"
          ]
        }
      },
      {
        "name": "gmail_get_labels",
        "toolReferenceName": "gmail-labels",
        "displayName": "List Gmail Labels",
        "icon": "$(tag)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List your Gmail labels and folders",
        "modelDescription": "Lists all Gmail labels (folders) including system and custom labels.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "account": {
              "type": "string",
              "description": "Account label. Omit for active account."
            }
          }
        }
      },
      {
        "name": "aws_s3_list_buckets",
        "toolReferenceName": "aws-s3-buckets",
        "displayName": "List S3 Buckets",
        "icon": "$(database)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List your AWS S3 buckets",
        "modelDescription": "Lists all S3 buckets in the user's AWS account. Returns bucket names and creation dates.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "aws_s3_list_objects",
        "toolReferenceName": "aws-s3-objects",
        "displayName": "List S3 Objects",
        "icon": "$(file)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List objects in an S3 bucket",
        "modelDescription": "Lists objects in an AWS S3 bucket, optionally filtered by prefix. Returns object keys and sizes.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "bucket": {
              "type": "string",
              "description": "S3 bucket name"
            },
            "prefix": {
              "type": "string",
              "description": "Optional key prefix to filter objects"
            },
            "maxKeys": {
              "type": "number",
              "description": "Max objects to return (default: 20)"
            }
          },
          "required": [
            "bucket"
          ]
        }
      },
      {
        "name": "aws_s3_get_object",
        "toolReferenceName": "aws-s3-get",
        "displayName": "Get S3 Object",
        "icon": "$(cloud-download)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Read content of an S3 object",
        "modelDescription": "Downloads and reads the content of an S3 object. Best for text files. Returns first 3000 chars of content.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "bucket": {
              "type": "string",
              "description": "S3 bucket name"
            },
            "key": {
              "type": "string",
              "description": "Object key (file path)"
            }
          },
          "required": [
            "bucket",
            "key"
          ]
        }
      },
      {
        "name": "aws_lambda_list_functions",
        "toolReferenceName": "aws-lambda-list",
        "displayName": "List Lambda Functions",
        "icon": "$(zap)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List your AWS Lambda functions",
        "modelDescription": "Lists Lambda functions in the user's AWS account. Returns function name, runtime, memory, and timeout.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "maxItems": {
              "type": "number",
              "description": "Max functions to return (default: 20)"
            }
          }
        }
      },
      {
        "name": "aws_lambda_invoke",
        "toolReferenceName": "aws-lambda-invoke",
        "displayName": "Invoke Lambda Function",
        "icon": "$(play)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Invoke an AWS Lambda function",
        "modelDescription": "Invokes a Lambda function with an optional JSON payload. Returns the function result. Confirm with user before invoking.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "functionName": {
              "type": "string",
              "description": "Lambda function name or ARN"
            },
            "payload": {
              "type": "string",
              "description": "JSON payload to send (default: '{}')"
            }
          },
          "required": [
            "functionName"
          ]
        }
      },
      {
        "name": "aws_ec2_list_instances",
        "toolReferenceName": "aws-ec2-list",
        "displayName": "List EC2 Instances",
        "icon": "$(server)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List your EC2 instances",
        "modelDescription": "Lists EC2 instances in the user's AWS account with instance ID, type, and state.",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "aws_ec2_manage_instance",
        "toolReferenceName": "aws-ec2-manage",
        "displayName": "Manage EC2 Instance",
        "icon": "$(settings-gear)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Start, stop, or reboot an EC2 instance",
        "modelDescription": "Start, stop, or reboot an EC2 instance. Requires instance ID and action. Always confirm with the user before executing.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "instanceId": {
              "type": "string",
              "description": "EC2 instance ID (e.g. i-1234567890abcdef0)"
            },
            "action": {
              "type": "string",
              "description": "Action: start, stop, or reboot",
              "enum": [
                "start",
                "stop",
                "reboot"
              ]
            }
          },
          "required": [
            "instanceId",
            "action"
          ]
        }
      },
      {
        "name": "aws_cloudwatch_log_groups",
        "toolReferenceName": "aws-cw-groups",
        "displayName": "List CloudWatch Log Groups",
        "icon": "$(output)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List CloudWatch log groups",
        "modelDescription": "Lists CloudWatch log groups, optionally filtered by prefix. Returns group name, storage size, and retention.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "prefix": {
              "type": "string",
              "description": "Optional log group name prefix filter"
            },
            "limit": {
              "type": "number",
              "description": "Max groups to return (default: 20)"
            }
          }
        }
      },
      {
        "name": "aws_cloudwatch_get_logs",
        "toolReferenceName": "aws-cw-logs",
        "displayName": "Get CloudWatch Logs",
        "icon": "$(list-flat)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Get recent CloudWatch log events",
        "modelDescription": "Fetches recent log events from a CloudWatch log group. If no stream specified, uses the latest stream. Returns timestamps and messages.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "logGroupName": {
              "type": "string",
              "description": "CloudWatch log group name"
            },
            "logStreamName": {
              "type": "string",
              "description": "Optional specific log stream name"
            },
            "limit": {
              "type": "number",
              "description": "Max events to return (default: 30)"
            }
          },
          "required": [
            "logGroupName"
          ]
        }
      },
      {
        "name": "vercel_list_projects",
        "toolReferenceName": "vercel-projects",
        "displayName": "List Vercel Projects",
        "icon": "$(project)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List your Vercel projects",
        "modelDescription": "Lists Vercel projects in the user's account. Returns project name, framework, and last update date.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "limit": {
              "type": "number",
              "description": "Max projects to return (default: 10)"
            }
          }
        }
      },
      {
        "name": "vercel_list_deployments",
        "toolReferenceName": "vercel-deployments",
        "displayName": "List Vercel Deployments",
        "icon": "$(cloud-upload)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List Vercel deployments",
        "modelDescription": "Lists recent Vercel deployments. Optionally filter by project name. Returns deployment name, state, URL, and date.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "projectName": {
              "type": "string",
              "description": "Optional project name to filter"
            },
            "limit": {
              "type": "number",
              "description": "Max deployments to return (default: 10)"
            }
          }
        }
      },
      {
        "name": "vercel_deployment_details",
        "toolReferenceName": "vercel-deploy-info",
        "displayName": "Vercel Deployment Details",
        "icon": "$(info)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Get details of a Vercel deployment",
        "modelDescription": "Gets full details of a specific Vercel deployment by ID. Returns name, URL, state, target, git commit, and branch.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "deploymentId": {
              "type": "string",
              "description": "Vercel deployment ID"
            }
          },
          "required": [
            "deploymentId"
          ]
        }
      },
      {
        "name": "vercel_list_domains",
        "toolReferenceName": "vercel-domains",
        "displayName": "List Vercel Domains",
        "icon": "$(globe)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List your Vercel domains",
        "modelDescription": "Lists all domains in the user's Vercel account with verification status and nameservers.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "limit": {
              "type": "number",
              "description": "Max domains to return (default: 20)"
            }
          }
        }
      },
      {
        "name": "vercel_list_env_vars",
        "toolReferenceName": "vercel-env",
        "displayName": "List Vercel Env Vars",
        "icon": "$(symbol-variable)",
        "canBeReferencedInPrompt": true,
        "userDescription": "List environment variables of a Vercel project",
        "modelDescription": "Lists environment variables for a Vercel project. Shows keys and targets but NOT values (for security). Use to check what env vars are configured.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "projectName": {
              "type": "string",
              "description": "Vercel project name"
            }
          },
          "required": [
            "projectName"
          ]
        }
      },
      {
        "name": "memory_add",
        "toolReferenceName": "memory-add",
        "displayName": "Remember Something",
        "icon": "$(lightbulb)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Save a fact or preference to persistent memory",
        "modelDescription": "Save a fact, preference, or lesson to persistent memory that survives across ALL sessions and VS Code windows. Use this PROACTIVELY when you learn: user preferences ('I prefer TypeScript'), environment facts ('runs macOS, has Docker'), project conventions ('uses tabs, 120-char lines'), corrections ('don't use sudo for Docker'), completed work ('migrated DB on 2026-01-15'). Two targets: 'memory' for agent notes (environment, conventions, lessons) and 'user' for user profile (name, preferences, communication style). Memory is injected into every conversation — keep entries concise and information-dense.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "target": {
              "type": "string",
              "description": "Where to store: 'memory' (agent notes — environment, conventions, lessons) or 'user' (user profile — preferences, style)",
              "enum": [
                "memory",
                "user"
              ]
            },
            "content": {
              "type": "string",
              "description": "Concise fact to remember. Pack multiple related facts into one entry. Example: 'User runs Windows 11, uses PowerShell, has Node 20 and Python 3.12'"
            }
          },
          "required": [
            "target",
            "content"
          ]
        }
      },
      {
        "name": "memory_remove",
        "toolReferenceName": "memory-remove",
        "displayName": "Forget Something",
        "icon": "$(trash)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Remove an entry from persistent memory",
        "modelDescription": "Remove a memory entry by providing a unique substring that identifies it. The substring must match exactly one entry. If it matches multiple, you'll be asked to be more specific. Use when information is outdated, wrong, or no longer relevant.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "target": {
              "type": "string",
              "description": "'memory' or 'user'",
              "enum": [
                "memory",
                "user"
              ]
            },
            "substring": {
              "type": "string",
              "description": "Unique substring that identifies the entry to remove (e.g. 'dark mode' to remove an entry about dark mode preference)"
            }
          },
          "required": [
            "target",
            "substring"
          ]
        }
      },
      {
        "name": "memory_replace",
        "toolReferenceName": "memory-replace",
        "displayName": "Update Memory",
        "icon": "$(edit)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Update an existing memory entry",
        "modelDescription": "Replace an existing memory entry with updated content. Use old_text as a unique substring to find the entry, and content as the full replacement. Preferred over remove+add when updating information (e.g. user changed their preferred editor, project switched frameworks).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "target": {
              "type": "string",
              "description": "'memory' or 'user'",
              "enum": [
                "memory",
                "user"
              ]
            },
            "old_text": {
              "type": "string",
              "description": "Unique substring identifying the entry to replace"
            },
            "content": {
              "type": "string",
              "description": "Full replacement text for the entry"
            }
          },
          "required": [
            "target",
            "old_text",
            "content"
          ]
        }
      },
      {
        "name": "memory_list",
        "toolReferenceName": "memory-list",
        "displayName": "Show Memory",
        "icon": "$(book)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Show all persistent memory entries",
        "modelDescription": "Lists all persistent memory entries with capacity usage. Shows both agent notes (MEMORY) and user profile (USER) by default, or filter by target. Use to check what's remembered before adding new entries, and to find entries for replacement or removal.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "target": {
              "type": "string",
              "description": "Optional: 'memory' or 'user' to show only one section. Omit to show both.",
              "enum": [
                "memory",
                "user"
              ]
            }
          }
        }
      },
      {
        "name": "session_search",
        "toolReferenceName": "session-search",
        "displayName": "Search Past Sessions",
        "icon": "$(search)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Search past tool sessions by keyword",
        "modelDescription": "Search across all past tool-call sessions by keyword. Returns matching tool calls with input/output excerpts and session IDs. Use when the user references something discussed or done earlier, e.g. 'remember that S3 bucket?', 'what Vercel project did I deploy?', 'find when I emailed John'. Also useful for your own recall — search before asking the user to repeat information.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Keyword(s) to search for in past tool calls"
            },
            "maxResults": {
              "type": "number",
              "description": "Max results to return (default: 15)"
            }
          },
          "required": [
            "query"
          ]
        }
      },
      {
        "name": "session_list",
        "toolReferenceName": "session-list",
        "displayName": "List Past Sessions",
        "icon": "$(history)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Browse recent tool sessions",
        "modelDescription": "Lists recent tool-call sessions sorted by most recent first. Each session shows start time, tool count, and a preview. Use to give the user an overview of recent activity, or to find a session ID for session_resume.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "limit": {
              "type": "number",
              "description": "Max sessions to list (default: 20)"
            }
          }
        }
      },
      {
        "name": "session_resume",
        "toolReferenceName": "session-resume",
        "displayName": "Resume Past Session",
        "icon": "$(debug-continue)",
        "canBeReferencedInPrompt": true,
        "userDescription": "Load full context from a past session",
        "modelDescription": "Loads the complete tool-call history from a past session. Returns every tool invocation with timestamps, inputs, and outputs. Use when the user says 'continue where we left off', 'resume that conversation', or when you need full context from a session found via session_search or session_list.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "sessionId": {
              "type": "string",
              "description": "Session ID to resume (get from session_list or session_search)"
            }
          },
          "required": [
            "sessionId"
          ]
        }
      }
    ]
  }
}
